# Merge these tables into ~/.codex/config.toml; do not replace the whole file.
# Requires the mail-manager-codex client in Keycloak (configure-mcp.ps1).
[mcp_servers.mailmanager]
url = "https://quentin-bouchot.fr/projets/MailManager/api/mcp"
default_tools_approval_mode = "writes"

[mcp_servers.mailmanager.oauth]
client_id = "mail-manager-codex"
callback_url = "http://127.0.0.1:5557/callback"
callback_port = 5557
